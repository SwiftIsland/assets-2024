//
//  Untitled.swift
//  SwiftIsland Delights
//
//  Created by Malin Sundberg on 2024-08-24.
//

// Example values to be used to create smooth animations
// Simple animation:
//let centerPointX: Float = Float(sin((timeline.date.timeIntervalSince1970) + 1)/2)
//let centerPointY: Float = Float(sin((timeline.date.timeIntervalSince1970) + 1)/2)

// More varied animation:
//let timeValue = timeline.date.timeIntervalSince1970 * 0.8
//let centerPointX: Float = Float((sin(timeValue) + cos(timeValue * 0.5) + sin(timeValue * 2)) / 6 + 0.5)
//let centerPointY: Float = Float((cos(timeValue + .pi/2) + sin(timeValue * 0.7) + cos(timeValue * 1.3)) / 6 + 0.5)
